---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App.tsx
  - app.json
  - package.json
  - tsconfig.json
  - babel.config.js
autonomous: true
requirements:
  - SHIFT-01
  - SHIFT-02
  - SHIFT-04
  - MILE-04
---

<objective>
Initialize React Native project with Expo for driver tracking app
</objective>

<context>
@.planning/phases/01-01-shift-mileage-tracking/01-CONTEXT.md

**Goal:** Drivers can log work shifts and track mileage driven

**Important:** Use "booking" or "mission" terminology — NOT "shift"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Expo project</name>
  <files>
    - App.tsx
    - app.json
    - package.json
  </files>
  <read_first>
- app.json (Expo config structure)
- package.json (dependencies)
</read_first>
  <action>
Create Expo project with:
- Project name: DriverTracking
- App name: Driver Tracking
- Bundle identifier: com.drivertracking.app

Create package.json with core dependencies:
- expo (~52)
- react-native
- @react-navigation/native
- @react-navigation/native-stack
- react-native-screens
- react-native-safe-area-context
- expo-secure-store (for auth token)
- @react-native-async-storage/async-storage (for theme/language - NOT expo-secure-store since SharedPreferences is mentioned in context)

Create app.json with:
- expo.name: "Driver Tracking"
- expo.slug: "driver-tracking"
- expo.version: "1.0.0"
- expo.android.package: "com.drivertracking.app"
- expo.ios.bundleIdentifier: "com.drivertracking.app"

Create App.tsx with:
- Basic navigation structure (React Navigation)
- Tab navigation (Home, Bookings, History, Profile)
- Loading/splash state

Create tsconfig.json and babel.config.js with standard Expo config.
  </action>
  <verify>
grep -q "@react-navigation/native" package.json
grep -q "expo" package.json
test -f App.tsx
  </verify>
  <done>
Expo project initialized with navigation structure, basic tabs, and core dependencies installed
</done>
  <acceptance_criteria>
- package.json contains @react-navigation/native, react-native, expo
- App.tsx contains navigation structure with at least 2 tabs
- app.json contains correct bundle identifier
- No TypeScript errors in the base project
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Set up project structure and routing</name>
  <files>
    - src/navigation/AppNavigator.tsx
    - src/screens/HomeScreen.tsx
    - src/screens/BookingsScreen.tsx
    - src/screens/HistoryScreen.tsx
    - src/screens/ProfileScreen.tsx
  </files>
  <read_first>
- App.tsx (created in task 1)
</read_first>
  <action>
Create src directory structure:
- src/navigation/ - navigation config
- src/screens/ - all app screens
- src/components/ - reusable components
- src/services/ - API services
- src/types/ - TypeScript types
- src/hooks/ - custom hooks
- src/utils/ - utility functions
- src/constants/ - app constants

Create AppNavigator.tsx with:
- Bottom Tab Navigator using @react-navigation/bottom-tabs
- Home tab → HomeScreen (dashboard)
- Bookings tab → BookingsScreen (active/pending bookings)
- History tab → HistoryScreen (completed/cancelled)
- Profile tab → ProfileScreen (driver info)

Create placeholder screens:
- HomeScreen: Shows driver name, active booking status, vehicle info placeholder
- BookingsScreen: Shows list of pending/in-progress bookings
- HistoryScreen: Shows completed/cancelled bookings with filter
- ProfileScreen: Shows driver info placeholder
  </action>
  <verify>
test -d src/navigation
test -d src/screens
test -f src/navigation/AppNavigator.tsx
grep -l "BottomTabNavigator" src/navigation/*.tsx
  </verify>
  <done>
Project directory structure created with navigation and placeholder screens
</done>
  <acceptance_criteria>
- src/navigation/AppNavigator.tsx exports tab navigator
- At least 4 screen files created in src/screens/
- src directory follows the structure defined above
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Set up API service layer</name>
  <files>
    - src/services/api.ts
    - src/services/auth.ts
    - src/types/index.ts
  </files>
  <read_first>
- .planning/phases/01-01-shift-mileage-tracking/01-CONTEXT.md (API endpoints)
</read_first>
  <action>
Create src/types/index.ts with TypeScript interfaces:
- Booking interface (id, ref, status, fk_driver, fk_vehicle, fk_customer, departure_address, arriving_address, pickup_datetime, dropoff_datetime, distance, eta)
- Driver interface (id, firstname, lastname, phone, email, license_info, driver_image, fk_vehicle)
- Vehicle interface (id, maker, model, license_plate, engine_type, color, year, vin, initial_mileage)
- Customer interface (id, firstname, lastname, phone, company_name)
- MileageRecord interface (meter_out, meter_in, fuel_out, fuel_in, date)

Create src/services/api.ts:
- Base URL configuration from CONTEXT: API is at /custom/drivertracking/
- Fetch wrapper with error handling
- Auth token management (stored via expo-secure-store)

Create src/services/auth.ts:
- Login function: POST to /custom/drivertracking/api/login
- Token storage/retrieval using expo-secure-store
- Logout function to clear token
- Token validation check
  </action>
  <verify>
grep -q "interface Booking" src/types/index.ts
grep -q "interface Driver" src/types/index.ts
test -f src/services/api.ts
test -f src/services/auth.ts
  </verify>
  <done>
API service layer set up with TypeScript types and authentication service
</done>
  <acceptance_criteria>
- src/types/index.ts exports at least 4 type interfaces
- src/services/api.ts contains base API config
- src/services/auth.ts contains login/logout/token functions
- All TypeScript types match the database table fields from CONTEXT.md
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input (booking IDs, auth tokens) crosses here via HTTP |
| API→Backend | App communicates with Dolibarr backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | Login endpoint | mitigate | Token stored in secure storage (expo-secure-store), never in plain text |
| T-01-02 | T | API requests | mitigate | Use HTTPS for all API calls (backend config) |
| T-01-03 | I | Booking IDs | mitigate | Validate booking ID is numeric before API call |
</threat_model>

<verification>
[Overall verification steps]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
After completion, create .planning/phases/01-01-shift-mileage-tracking/01-01-SUMMARY.md
</output>