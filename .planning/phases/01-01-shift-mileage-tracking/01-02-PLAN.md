---
phase: 01
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/screens/BookingsScreen.tsx
  - src/screens/BookingDetailScreen.tsx
  - src/screens/HomeScreen.tsx
  - src/services/api.ts
  - src/components/BookingCard.tsx
autonomous: true
requirements:
  - SHIFT-01
  - SHIFT-02
  - SHIFT-04
---

<objective>
Implement booking management (list, detail, start/complete actions)
</objective>

<context>
@.planning/phases/01-01-shift-mileage-tracking/01-CONTEXT.md
@.planning/phases/01-01-shift-mileage-tracking/01-01-PLAN.md

**Important:** Use "booking" or "mission" terminology — NOT "shift"

**Booking status flow:** `confirmed` → `in_progress` → `done` → `cancelled`

**API endpoints:**
- GET /custom/drivertracking/api_driver.php — full dashboard data
- POST ajax_start_booking.php — start booking (set status to in_progress)
- POST ajax_update_booking.php — complete/cancel/undo booking
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement dashboard/Home screen with active booking</name>
  <files>
    - src/screens/HomeScreen.tsx
    - src/services/api.ts
  </files>
  <read_first>
- src/types/index.ts (types from Plan 01-01)
- src/services/api.ts (API layer from Plan 01-01)
- .planning/phases/01-01-shift-mileage-tracking/01-CONTEXT.md (API endpoints)
</read_first>
  <action>
Update HomeScreen.tsx to fetch and display:
- Driver info (name from auth context)
- Active booking (status = in_progress) - shows booking details:
  - Pickup/dropoff addresses
  - Customer name
  - Distance (from llx_flotte_booking.distance)
  - ETA
- Vehicle info (maker, model, license_plate)
- "Start Booking" button if no active booking

Add to api.ts:
- getDashboard() function: GET /custom/drivertracking/api_driver.php
- Returns: driver info, active booking, vehicle info, pending bookings list
- Handle 401 errors (token expired) - redirect to login

Create useDashboard hook in src/hooks/useDashboard.ts:
- Fetches dashboard data on mount
- Provides activeBooking, driverInfo, vehicleInfo states
- Provides refetch function
  </action>
  <verify>
grep -q "getDashboard" src/services/api.ts
grep -q "activeBooking" src/screens/HomeScreen.tsx
test -f src/hooks/useDashboard.ts
  </verify>
  <done>
Home screen displays driver info, vehicle info, and active booking status
</done>
  <acceptance_criteria>
- HomeScreen.tsx renders driver name
- HomeScreen shows vehicle info (maker, model, license_plate)
- If active booking exists: shows pickup/dropoff addresses, distance, status
- If no active booking: shows "Start Booking" prompt
- API call to /custom/drivertracking/api_driver.php is implemented
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement bookings list screen</name>
  <files>
    - src/screens/BookingsScreen.tsx
    - src/components/BookingCard.tsx
  </files>
  <read_first>
- src/types/index.ts
- src/services/api.ts
- src/hooks/useDashboard.ts (from task 1)
</read_first>
  <action>
Update BookingsScreen.tsx to display:
- Tab/segment control: "Pending" | "In Progress"
- List of bookings in each tab
- Pull-to-refresh functionality

Create BookingCard.tsx component:
- Displays: booking ref, pickup → dropoff addresses, customer name, distance, ETA
- Status badge: "Pending" (yellow), "In Progress" (blue)
- Tap action: navigate to BookingDetailScreen

Add to api.ts:
- getBookings(status?: string) function: GET /api/index.php/flotte/bookings
- Filter by status parameter (confirmed/in_progress)

Add "Empty state" component for when no bookings in a category
  </action>
  <verify>
grep -q "BookingCard" src/screens/BookingsScreen.tsx
grep -q "getBookings" src/services/api.ts
test -f src/components/BookingCard.tsx
  </verify>
  <done>
Bookings screen shows pending and in-progress bookings list
</done>
  <acceptance_criteria>
- BookingsScreen has tabs for Pending/In Progress
- Each tab shows filtered bookings
- BookingCard shows: ref, addresses, customer, distance, status badge
- Pull-to-refresh triggers API call
- Empty state shown when no bookings
</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement booking detail screen with start/complete actions</name>
  <files>
    - src/screens/BookingDetailScreen.tsx
    - src/services/api.ts
  </files>
  <read_first>
- src/screens/BookingsScreen.tsx (navigation setup)
- src/services/api.ts
- .planning/phases/01-01-shift-mileage-tracking/01-CONTEXT.md (API calls)
</read_first>
  <action>
Create BookingDetailScreen.tsx:
- Header: booking ref, status badge
- Section: Pickup location (address, datetime)
- Section: Dropoff location (address, datetime)
- Section: Customer info (name, phone - tappable to call)
- Section: Distance (read from llx_flotte_booking.distance)
- Section: Vehicle info (if assigned)

Action buttons at bottom:
- If status = "confirmed": "Start Booking" button (primary)
- If status = "in_progress": "Complete Booking" button (primary), "Cancel" button (secondary)
- If status = "done" or "cancelled": View only, no actions

Add to api.ts:
- startBooking(booking_id) function: POST /custom/drivertracking/ajax_start_booking.php with {booking_id}
  - Backend auto-captures timestamp - no manual time entry
- updateBookingStatus(booking_id, action) function: POST /custom/drivertracking/ajax_update_booking.php with {booking_id, action}
  - actions: "complete", "cancel", "undo"
- Handle response: update local state, navigate back on success

Use React Navigation: Stack.Screen with params (booking ID)
  </action>
  <verify>
grep -q "startBooking" src/services/api.ts
grep -q "updateBookingStatus" src/services/api.ts
grep -q "BookingDetailScreen" src/screens/BookingsScreen.tsx
  </verify>
  <done>
Booking detail screen allows starting and completing bookings
</done>
  <acceptance_criteria>
- BookingDetailScreen shows all booking fields (ref, addresses, customer, distance, datetime)
- "Start Booking" button appears for confirmed bookings
- "Complete" and "Cancel" buttons appear for in_progress bookings
- Phone number is tappable (opens dialer)
- startBooking calls POST ajax_start_booking.php with booking_id
- updateBookingStatus calls POST ajax_update_booking.php with action
- Error handling: show alert on failure
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input (booking IDs, status changes) crosses here |
| API→Backend | Booking status changes via custom endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | I | Booking ID in start/complete | mitigate | Validate booking_id is numeric integer before API call |
| T-01-05 | S | Status transition logic | mitigate | Only allow confirmed→in_progress and in_progress→done/cancel |
| T-01-06 | D | Duplicate actions | mitigate | Disable buttons during API call (loading state) |
</threat_model>

<verification>
[Overall verification steps]
</verification>

<success_criteria>
[Measurable completion]
</success_criteria>

<output>
After completion, create .planning/phases/01-01-shift-mileage-tracking/01-02-SUMMARY.md
</output>