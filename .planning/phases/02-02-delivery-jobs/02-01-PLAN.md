---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/LoginScreen.tsx
  - src/services/auth.ts
  - src/App.tsx
autonomous: true
requirements:
  - JOB-01
  - JOB-02

must_haves:
  truths:
    - User can enter server URL, username, and password
    - User can tap Login and receive DOLAPIKEY token from POST /api/index.php/login
    - Token is stored in expo-secure-store under key "auth_token"
    - On successful login, user is redirected to tab navigator (HomeScreen)
    - On failed login, error message is displayed
    - On app launch with valid token, user goes directly to HomeScreen (no login needed)
    - On app launch with no token, user sees LoginScreen
  artifacts:
    - path: src/screens/LoginScreen.tsx
      provides: Login UI with server URL, username, password inputs and Login button
      exports: LoginScreen component
    - path: src/services/auth.ts
      provides: Token storage, session persistence, logout function
      exports: storeToken, getToken, clearToken, isAuthenticated
    - path: src/App.tsx
      provides: Root navigator with auth state routing
      exports: App with conditional LoginScreen/HomeScreen rendering
  key_links:
    - from: src/screens/LoginScreen.tsx
      to: src/services/auth.ts
      via: storeToken() call on successful login
      pattern: "storeToken.*POST.*login"
    - from: src/App.tsx
      to: src/services/auth.ts
      via: useEffect on mount checks getToken()
      pattern: "getToken.*isAuthenticated"
---

<objective>
Build login screen with authentication token flow. Driver can log in with server URL, username, password and receive DOLAPIKEY token.
</objective>

<context>
@.planning/phases/02-02-delivery-jobs/02-CONTEXT.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoginScreen.tsx</name>
  <files>src/screens/LoginScreen.tsx</files>
  <read_first>
    - src/App.tsx (current navigation structure)
    - src/services/auth.ts (existing auth functions)
    - src/services/api.ts (configured endpoint base)
  </read_first>
  <action>
Create LoginScreen.tsx with:
- State: serverUrl (string), username (string), password (string), error (string), loading (boolean)
- TextInputs: Server URL (placeholder: "https://yourserver.com"), Username, Password
- Login button: calls POST /api/index.php/login with body { login: username, password: password }
- On 200 success: Extract token from response, call storeToken(token), call setItem('server_url', serverUrl), navigate to HomeScreen
- On failure: Set error state, display alert with error message
- Use existing styling from Phase 1 screens (colors, spacing, typography)
- Input validation: All 3 fields required, serverUrl must start with http
  </action>
  <verify>
    <automated>grep -c "TextInput" src/screens/LoginScreen.tsx</automated>
  </verify>
  <done>
    LoginScreen displays with server URL, username, password fields, and Login button
    Login button triggers POST /api/index.php/login on press
    On success: token stored, navigate to HomeScreen
    On failure: error message displayed
  </done>
</task>

<task type="auto">
  <name>Task 2: Update auth.ts with token storage functions</name>
  <files>src/services/auth.ts</files>
  <read_first>
    - src/services/auth.ts (existing file)
  </read_first>
  <action>
Update src/services/auth.ts:
- Import * as SecureStore from 'expo-secure-store'
- storeToken(token: string): Promise<void> - stores under key 'auth_token'
- getToken(): Promise<string | null> - retrieves token
- clearToken(): Promise<void> - removes token
- isAuthenticated(): Promise<boolean> - returns true if token exists
- storeServerUrl(url: string): stores in AsyncStorage key 'server_url'
- getServerUrl(): Promise<string | null> - retrieves server URL
- logout(): calls clearToken(), navigate to LoginScreen
  </action>
  <verify>
    <automated>grep -c "SecureStore" src/services/auth.ts</automated>
  </verify>
  <done>
    Token stored securely in expo-secure-store under 'auth_token'
    Server URL stored in AsyncStorage under 'server_url'
    logout() clears token and redirects to login
  </done>
</task>

<task type="auto">
  <name>Task 3: Update App.tsx for auth routing</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current navigation)
    - src/services/auth.ts (token functions)
  </read_first>
  <action>
Update src/App.tsx:
- Add state: isLoading (boolean), isAuthenticated (boolean)
- Add useEffect on mount: call isAuthenticated() to check token
- If token exists: set isAuthenticated(true), render TabNavigator (HomeScreen)
- If no token: set isAuthenticated(false), render LoginScreen
- After successful login in LoginScreen: update App state to show HomeScreen
- Import and render LoginScreen component when not authenticated
  </action>
  <verify>
    <automated>grep -c "isAuthenticated" src/App.tsx</automated>
  </verify>
  <done>
    App launch checks for token in secure storage
    Valid token → HomeScreen shown immediately (no login screen)
    No token → LoginScreen shown
    Session persists across app restarts
  </done>
</task>

</tasks>

<verification>
- LoginScreen renders with 3 input fields and button
- Login POST call uses correct endpoint and body format
- Token stored in expo-secure-store (not AsyncStorage for security)
- App routing based on auth state works
</verification>

<success_criteria>
- User can log in with valid credentials and see HomeScreen
- User sees error on invalid credentials
- App remembers login across restarts
- Logout clears token and shows LoginScreen
</success_criteria>

<output>
After completion, create .planning/phases/02-02-delivery-jobs/02-01-SUMMARY.md
</output>