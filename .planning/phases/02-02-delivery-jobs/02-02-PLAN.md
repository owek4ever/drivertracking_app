---
phase: 02
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/services/api.ts
  - src/screens/HomeScreen.tsx
  - src/screens/BookingsScreen.tsx
  - src/screens/BookingDetailScreen.tsx
  - src/screens/HistoryScreen.tsx
autonomous: true
requirements:
  - JOB-01
  - JOB-02
  - JOB-03
  - JOB-04
  - JOB-05

must_haves:
  truths:
    - HomeScreen fetches from GET /custom/drivertracking/api_driver.php with DOLAPIKEY header
    - BookingsScreen fetches from GET /api/index.php/flotte/bookings?status=confirmed
    - BookingDetailScreen fetches from GET /api/index.php/flotte/bookings/{id}
    - HistoryScreen fetches from GET /api/index.php/flotte/bookings (all statuses)
    - All API calls include DOLAPIKEY: {token} header
    - 401 response triggers: clearToken(), show alert "Session expired. Please log in again.", navigate to LoginScreen
    - Network errors show alert "Cannot connect to server. Check your connection."
    - All list screens have pull-to-refresh functionality
  artifacts:
    - path: src/services/api.ts
      provides: API client with auth header injection, 401 handling, error handling
      exports: apiClient with get/post methods
    - path: src/hooks/useDashboard.ts
      provides: Fetch and manage dashboard data from API
      exports: useDashboard hook
    - path: src/hooks/useBookings.ts
      provides: Fetch and manage bookings list with status filtering
      exports: useBookings hook
    - path: src/screens/HomeScreen.tsx
      provides: Dashboard with real data from API
      exports: HomeScreen component
    - path: src/screens/BookingsScreen.tsx
      provides: Bookings list with pull-to-refresh
      exports: BookingsScreen component
    - path: src/screens/BookingDetailScreen.tsx
      provides: Single booking details from API
      exports: BookingDetailScreen component
    - path: src/screens/HistoryScreen.tsx
      provides: Completed bookings history with pull-to-refresh
      exports: HistoryScreen component
  key_links:
    - from: src/services/api.ts
      to: src/services/auth.ts
      via: getToken() for header injection
      pattern: "DOLAPIKEY.*getToken"
    - from: src/screens/*
      to: src/services/api.ts
      via: apiClient.get() calls
      pattern: "apiClient\\.get"
    - from: api.ts (401 handler)
      to: src/screens/LoginScreen.tsx
      via: Navigation ref or callback
      pattern: "navigate.*Login"
---

<objective>
Wire all screens to live API endpoints. Handle 401 errors globally. Implement pull-to-refresh on all list screens.
</objective>

<context>
@.planning/phases/02-02-delivery-jobs/02-CONTEXT.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-02-delivery-jobs/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update api.ts with auth header and 401 handling</name>
  <files>src/services/api.ts</files>
  <read_first>
    - src/services/api.ts (existing)
    - src/services/auth.ts (token functions from 02-01)
  </read_first>
  <action>
Update src/services/api.ts:
- Import getToken, clearToken, getServerUrl from auth.ts
- Create apiClient object with:
  - baseURL: dynamically get from getServerUrl()
  - get(endpoint): calls getToken(), adds header 'DOLAPIKEY: {token}', performs fetch
  - post(endpoint, data): same auth header injection
- Add global error handler:
  - On 401 response: clearToken(), show alert("Session expired. Please log in again."), trigger navigation to LoginScreen
  - On network error: show alert("Cannot connect to server. Check your connection.")
- Use axios or fetch - prefer axios if already installed, otherwise fetch
  </action>
  <verify>
    <automated>grep -c "DOLAPIKEY" src/services/api.ts</automated>
  </verify>
  <done>
    All API calls include DOLAPIKEY header from stored token
    401 responses automatically clear token and prompt re-login
    Network errors display user-friendly message
  </done>
</task>

<task type="auto">
  <name>Task 2: Create useDashboard and useBookings hooks</name>
  <files>
    - src/hooks/useDashboard.ts
    - src/hooks/useBookings.ts
  </files>
  <read_first>
    - src/services/api.ts
    - src/screens/HomeScreen.tsx (existing stub)
    - src/screens/BookingsScreen.tsx (existing stub)
  </read_first>
  <action>
Create src/hooks/useDashboard.ts:
- Function: useDashboard() returns { data, loading, error, refetch }
- Calls GET /custom/drivertracking/api_driver.php
- Returns dashboard data for HomeScreen

Create src/hooks/useBookings.ts:
- Function: useBookings(status?: string) returns { bookings, loading, error, refetch }
- If status provided: GET /api/index.php/flotte/bookings?status={status}
- If no status: GET /api/index.php/flotte/bookings (all)
- Returns bookings array for BookingsScreen and HistoryScreen
  </action>
  <verify>
    <automated>ls src/hooks/*.ts 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    useDashboard hook fetches from /custom/drivertracking/api_driver.php
    useBookings hook fetches from /api/index.php/flotte/bookings with optional status filter
    Both hooks provide loading, error, refetch for UI
  </done>
</task>

<task type="auto">
  <name>Task 3: Update HomeScreen to use real API</name>
  <files>src/screens/HomeScreen.tsx</files>
  <read_first>
    - src/screens/HomeScreen.tsx (existing stub)
    - src/hooks/useDashboard.ts
  </read_first>
  <action>
Update HomeScreen.tsx:
- Import useDashboard hook
- Replace stub data with: const { data, loading, error, refetch } = useDashboard()
- Display loading indicator while loading
- Display error message if error
- Render dashboard with real data from API
- Add pull-to-refresh: ScrollView with refreshControl calling refetch()
  </action>
  <verify>
    <automated>grep -c "useDashboard" src/screens/HomeScreen.tsx</automated>
  </verify>
  <done>
    HomeScreen displays real data from GET /custom/drivertracking/api_driver.php
    Pull-to-refresh updates dashboard data
  </done>
</task>

<task type="auto">
  <name>Task 4: Update BookingsScreen to use real API</name>
  <files>src/screens/BookingsScreen.tsx</files>
  <read_first>
    - src/screens/BookingsScreen.tsx (existing stub)
    - src/hooks/useBookings.ts
  </read_first>
  <action>
Update BookingsScreen.tsx:
- Import useBookings hook
- Call useBookings('confirmed') for confirmed bookings
- Replace stub data with: const { bookings, loading, error, refetch } = useBookings('confirmed')
- Display loading indicator while loading
- Display error message if error
- Render FlatList with real booking items
- Add pull-to-refresh: FlatList with refreshControl calling refetch()
  </action>
  <verify>
    <automated>grep -c "useBookings" src/screens/BookingsScreen.tsx</automated>
  </verify>
  <done>
    BookingsScreen shows confirmed bookings from API
    Pull-to-refresh updates bookings list
  </done>
</task>

<task type="auto">
  <name>Task 5: Update BookingDetailScreen to use real API</name>
  <files>src/screens/BookingDetailScreen.tsx</files>
  <read_first>
    - src/screens/BookingDetailScreen.tsx (existing stub)
    - src/services/api.ts
  </read_first>
  <action>
Update BookingDetailScreen.tsx:
- Get booking ID from route params (React Navigation)
- Call GET /api/index.php/flotte/bookings/{id} using apiClient
- Store booking in state: booking, loading, error
- Display loading indicator while loading
- Display error message if error
- Render booking details: address, customer, time, status
- If booking not found: show "Booking not found" message
  </action>
  <verify>
    <automated>grep -c "flotte/bookings" src/screens/BookingDetailScreen.tsx</automated>
  </verify>
  <done>
    BookingDetailScreen shows full details for selected booking from API
  </done>
</task>

<task type="auto">
  <name>Task 6: Update HistoryScreen to use real API</name>
  <files>src/screens/HistoryScreen.tsx</files>
  <read_first>
    - src/screens/HistoryScreen.tsx (existing stub)
    - src/hooks/useBookings.ts
  </read_first>
  <action>
Update HistoryScreen.tsx:
- Import useBookings hook
- Call useBookings() without status filter (gets all bookings)
- Replace stub data with: const { bookings, loading, error, refetch } = useBookings()
- Display loading indicator while loading
- Display error message if error
- Render FlatList with all bookings (completed history)
- Add pull-to-refresh: FlatList with refreshControl calling refetch()
  </action>
  <verify>
    <automated>grep -c "useBookings" src/screens/HistoryScreen.tsx</automated>
  </verify>
  <done>
    HistoryScreen shows all bookings from API
    Pull-to-refresh updates history list
  </done>
</task>

</tasks>

<verification>
- All 4 screens use real API endpoints
- DOLAPIKEY header sent with all requests
- 401 triggers logout and redirect to LoginScreen
- Pull-to-refresh works on BookingsScreen and HistoryScreen
</verification>

<success_criteria>
- All screens show real data from backend
- Authentication persists across app restarts
- Session expiry handled gracefully with re-login prompt
- Network errors show user-friendly messages
</success_criteria>

<output>
After completion, create .planning/phases/02-02-delivery-jobs/02-02-SUMMARY.md
</output>