---
phase: 03-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - Driver has emergency contacts visible on HomeScreen
    - Driver can call dispatch with one tap
    - Driver can call customer with one tap
    - Driver can call manager with one tap
  artifacts:
    - path: src/components/EmergencyContacts.tsx
      provides: Emergency contacts UI with quick-call buttons
    - path: src/screens/HomeScreen.tsx
      provides: HomeScreen with emergency contacts integrated
  key_links:
    - from: src/components/EmergencyContacts.tsx
      to: HomeScreen
      via: Component import and usage
      pattern: EmergencyContacts
---

<objective>
Add emergency contacts with one-tap calling functionality.

Purpose: Drivers can quickly call dispatch, customer, or manager during missions
Output: EmergencyContacts component integrated into HomeScreen
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-01-ui-polish-emergency-contacts/03-CONTEXT.md

From src/screens/BookingDetailScreen.tsx (line 165):
```typescript
Linking.openURL(`tel:${customer.phone}`);
```

This pattern is already used in the codebase for one-tap calling.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmergencyContacts component</name>
  <files>src/components/EmergencyContacts.tsx</files>
  <action>
Create EmergencyContacts.tsx component:
- Accept props: dispatchPhone, customerPhone?, managerPhone
- Render 3 TouchableOpacity buttons in a row
- Each button: phone icon + label + number
- On press: Linking.openURL('tel:PHONE')
- Style: Card with white background, rounded corners, iOS-style
- If customerPhone not provided, show button as disabled/dimmed
  </action>
  <verify>
    <automated>
grep -c "Linking.openURL" src/components/EmergencyContacts.tsx
    </automated>
  </verify>
  <done>EmergencyContacts component exists with 3 call buttons</done>
</task>

<task type="auto">
  <name>Task 2: Integrate EmergencyContacts into HomeScreen</name>
  <files>src/screens/HomeScreen.tsx</files>
  <action>
Update HomeScreen.tsx:
- Import EmergencyContacts from ../components/EmergencyContacts
- Get customerPhone from activeBooking?.customer?.phone (or pendingBookings[0])
- Get dispatchPhone: use default "1234567890" or from config (hardcode for now)
- Get managerPhone: use default "0987654321" or from driver profile
- Add EmergencyContacts component below Vehicle Card in the ScrollView
- Wrap in View with marginTop: 12
  </action>
  <verify>
    <automated>
grep -c "EmergencyContacts" src/screens/HomeScreen.tsx
    </automated>
  </verify>
  <done>HomeScreen displays emergency contacts section</done>
</task>

</tasks>

<verification>
- [ ] Emergency contacts section appears on HomeScreen
- [ ] Three buttons visible: Dispatch, Customer, Manager
- [ ] Tapping a button opens phone dialer with correct number
- [ ] Customer button disabled when no active booking
</verification>

<success_criteria>
Driver can see emergency contacts on HomeScreen and call any contact with one tap
</success_criteria>

<output>
After completion, create .planning/phases/03-01-ui-polish-emergency-contacts/03-01-SUMMARY.md
</output>