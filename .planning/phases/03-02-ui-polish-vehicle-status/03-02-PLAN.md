---
phase: 03-ui-polish
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: []
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - Driver can see vehicle mileage bar on HomeScreen
    - Driver can see vehicle fuel level bar on HomeScreen
    - Driver can see task schedule (pending bookings) on HomeScreen
    - Driver can toggle between dark and light theme
  artifacts:
    - path: src/components/VehicleStatusBars.tsx
      provides: Vehicle status bars UI (mileage + fuel)
    - path: src/components/TaskSchedule.tsx
      provides: Task schedule list for HomeScreen
    - path: src/context/ThemeContext.tsx
      provides: Theme state management
    - path: src/screens/HomeScreen.tsx
      provides: HomeScreen with vehicle bars, task schedule, theme toggle
  key_links:
    - from: src/components/VehicleStatusBars.tsx
      to: useDashboard hook
      via: Props: mileage, vehicleInfo
    - from: src/components/TaskSchedule.tsx
      to: useDashboard hook
      via: Props: pendingBookings
    - from: src/context/ThemeContext.tsx
      to: All screens
      via: useTheme hook
---

<objective>
Add vehicle status bars, task schedule, and theme toggle to the app.

Purpose: Complete UI polish with vehicle monitoring, task overview, and theme customization
Output: Enhanced HomeScreen with all UI-02, UI-03, UI-04 features
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-01-ui-polish-emergency-contacts/03-CONTEXT.md

From src/components/MileageDisplay.tsx (existing):
```typescript
const SERVICE_INTERVAL = 5000;
const currentMileage = mileage?.meter_out ?? null;
const calculateProgress = () => { ... }
```

This pattern will be reused for fuel level display.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VehicleStatusBars component</name>
  <files>src/components/VehicleStatusBars.tsx</files>
  <action>
Create VehicleStatusBars.tsx component:
- Accept props: mileage (MileageRecord | null), vehicleInfo (Vehicle | null)
- Render TWO status bars:
  1. Mileage bar (reuse logic from MileageDisplay)
  2. Fuel level bar (new): show fuel_percentage from vehicleInfo, color-coded
- Fuel colors: green (>50%), yellow (20-50%), red (<20%)
- Use View with progress bar pattern similar to MileageDisplay
- Style: Card with white background, rounded corners
  </action>
  <verify>
    <automated>
grep -c "fuel_level\|fuel_percentage" src/components/VehicleStatusBars.tsx
    </automated>
  </verify>
  <done>VehicleStatusBars component exists with mileage and fuel bars</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskSchedule component</name>
  <files>src/components/TaskSchedule.tsx</files>
  <action>
Create TaskSchedule.tsx component:
- Accept props: bookings (Booking[])
- Show next 3-5 pending bookings as vertical list
- Each item shows: time (scheduled date), destination (arriving_address), customer name
- If no bookings, show "No pending tasks" message
- Tap item → call onPress callback with bookingId (for navigation)
- Style: Card with white background, scrollable if many items
  </action>
  <verify>
    <automated>
grep -c "pendingBookings\|arriving_address" src/components/TaskSchedule.tsx
    </automated>
  </verify>
  <done>TaskSchedule component exists with booking list</done>
</task>

<task type="auto">
  <name>Task 3: Create ThemeContext for theme management</name>
  <files>src/context/ThemeContext.tsx</files>
  <action>
Create ThemeContext.tsx:
- Use React.createContext with { theme, isDark, toggleTheme }
- useColorScheme() for system preference detection
- AsyncStorage for persisting user preference ('light' | 'dark' | 'system')
- Provide useTheme() hook for consumers
- Default to 'system' preference
  </action>
  <verify>
    <automated>
grep -c "useColorScheme\|AsyncStorage" src/context/ThemeContext.tsx
    </automated>
  </verify>
  <done>ThemeContext exists with theme state and toggle function</done>
</task>

<task type="auto">
  <name>Task 4: Update HomeScreen with all new components</name>
  <files>src/screens/HomeScreen.tsx</files>
  <action>
Update HomeScreen.tsx to include:
1. Import and use ThemeContext - wrap content in ThemeProvider or use useTheme
2. Add VehicleStatusBars component below EmergencyContacts
3. Add TaskSchedule component showing pendingBookings
4. Add theme toggle button in header (headerRight)
5. Apply theme colors dynamically based on isDark

Note: Import VehicleStatusBars and TaskSchedule. The theme toggle should be in the header as a touchable icon.
  </action>
  <verify>
<automated>
grep -c "VehicleStatusBars\|TaskSchedule\|ThemeContext" src/screens/HomeScreen.tsx
</automated>
  </verify>
  <done>HomeScreen displays vehicle bars, task schedule, and theme toggle</done>
</task>

</tasks>

<verification>
- [ ] Vehicle mileage bar visible on HomeScreen
- [ ] Vehicle fuel level bar visible with color coding
- [ ] Task schedule shows pending bookings
- [ ] Theme toggle switches between light/dark
- [ ] Theme preference persists across app restarts
</verification>

<success_criteria>
Driver can see vehicle status (mileage + fuel), view task schedule, and toggle theme
</success_criteria>

<output>
After completion, create .planning/phases/03-02-ui-polish-vehicle-status/03-02-SUMMARY.md
</output>